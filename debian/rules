#!/usr/bin/make -f

%:
	dh $@ --buildsystem=cmake --parallel

# web documentation uses .xml extension
override_dh_compress:
	dh_compress -X.xml

override_dh_installdocs:
	dh_installdocs -Xlicense.xml
