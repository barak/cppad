# Upstream is using a new format for the documentation which we cannot
# process, so we're keeping an empty -doc package for now, and will
# re-populate it once the appropriate toolchain is available.
libcppad-doc binary: empty-binary-package
